import { IDataAdapter } from '../interfaces/IDataAdapter';
export type IDList = {
    creationTime: number;
    fileID: string;
    ids: Record<string, boolean>;
    readBytes: number;
    url: string;
};
export type IDListsLookupResponse = Record<string, {
    url?: string;
    fileID?: string;
    creationTime: number;
    size?: number;
}>;
export type IDListsLookupBootstrap = string[];
export default abstract class IDListUtil {
    static parseLookupResponse(input: unknown): IDListsLookupResponse | null;
    static parseBootstrapLookup(input: string | object): IDListsLookupBootstrap | null;
    static updateIdList(lists: Record<string, IDList>, name: string, data: string): void;
    static removeOldIdLists(lists: Record<string, IDList>, lookup: IDListsLookupResponse): void;
    static saveToDataAdapter(hashedSDKKey: string, dataAdapter: IDataAdapter, lists: Record<string, IDList>, lookup: IDListsLookupResponse): Promise<void>;
}
