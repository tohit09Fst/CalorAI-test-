import { StatsigUser } from '../StatsigUser';
export declare const MAX_OBJ_SIZE = 4096;
export default class LogEventValidator {
    static validateEventName(eventName: string): string | null;
    static validateUserObject(user: StatsigUser): StatsigUser | null;
    static validateEventValue(value: string | number | null): string | number | null;
    static validateEventMetadata(metadata: Record<string, unknown> | null): Record<string, unknown> | null;
    static validateEventTime(time: number): number | null;
    private static shouldTrimParam;
    static approximateObjectSize(x: object): number;
}
